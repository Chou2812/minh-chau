"""Huong dan tao Github
khi nhan duoc repo (nguoi ta day source code len) lam sao de lay source code ve
Code > SSH (copy URL)
1. vao terminal: go git clone + URL cua SSH de lay source code ve
2. Up source len git: 
git log de liet ke nhung lan commit git
git diff : se thay nhung noi dung da sua (tru la nhung cai da xoa, + la nhung cai da them)
git status: cho biet nhung file dang sua
git commit -a (up tat ca cac file) -m "Update git"
git push origin master
git pull origin master: cap nhat du lieu tren nhanh master, lay source code moi nhat tu tren github ve may
"""
#
"""
vao git help de doc nhung comandline
"""
#
"""
git config --local user.name "dienten"
git config --local user.email
"""
#nguon tai lieu - selenium framework